# 基于 Flowable Work 的工作流平台项目设计文档

---

## 文档信息

| 项目名称 | 工作流管理平台 |
|---------|---------------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-04-30 |
| 技术方案 | **Flowable 8 + Spring Boot 4 + JDK 25** |
| 前端方案 | **Flowable Work 官方 UI（嵌入式）** |

---

## 一、项目背景与目标

### 1.1 背景

企业内部存在大量需要流程审批的业务场景（如请假、报销、采购审批、客户工单处理等），需要一个统一的工作流平台来：

- **标准化业务流程**：将线下审批流程线上化
- **提升效率**：减少人工流转时间，任务自动分配
- **可追溯**：完整的流程审计日志

### 1.2 目标

构建一套基于 Flowable 的企业级工作流平台：

- ✅ 提供可视化流程建模能力
- ✅ 任务自动分配与流转
- ✅ 待办任务收件箱
- ✅ 流程监控与报表
- ✅ 支持与现有客户系统集成

---

## 二、技术选型

### 2.1 核心决策：为什么选择 Flowable Work 官方 UI？

| 对比维度 | **选择 Flowable Work 官方 UI** | 自研/第三方管理界面 |
|---------|-------------------------------|-------------------|
| **与 Flowable 8 兼容性** | ✅ 100% 官方适配 | ⚠️ 需自行验证 |
| **功能完整性** | ✅ 全覆盖（BPMN/CMMN/DMN/表单） | ❌ 通常只有流程设计器 |
| **Spring Boot 4 + JDK 25** | ✅ 官方已测试适配 | ❌ 不确定性高 |
| **维护与更新** | ✅ 官方定期发布 | ⚠️ 社区维护，更新慢 |
| **移动端支持** | ✅ 官方 iOS/Android App | ❌ 通常无 |
| **企业功能** | ✅ REST API、审计、报表 | ❌ 缺失 |
| **开发成本** | ✅ 开箱即用，零前端开发 | ❌ 需要自研 |

### 2.2 技术栈清单

| 分类 | 技术 | 版本 |
|------|------|------|
| **后端框架** | Spring Boot | 4.0.6 |
| **Java版本** | JDK | 25 |
| **流程引擎** | Flowable | 8.x |
| **前端（官方UI）** | Flowable Work UI | 集成在 flowable-work-frontend |
| **前端扩展** | React + TypeScript + @flowable/work-scripts | 官方工具链 |
| **数据库** | MySQL / PostgreSQL | 8.0+ |
| **部署** | Docker / Kubernetes | - |

### 2.3 Flowable Work UI 核心功能模块

Flowable Work 是官方开箱即用的企业级 Web 应用，包含以下功能模块：

| 模块 | 功能 | 日常使用场景 |
|------|------|-------------|
| **New** | 快速创建流程/任务/案例 | 发起新的审批申请 |
| **Work** | 流程/案例实例管理 | 查看所有运行中的流程，跟踪进度 |
| **Tasks** | 任务收件箱 | 待办任务、已办任务、任务认领、任务完成 |
| **Contacts** | 联系人管理 | 管理用户、团队、组织架构 |
| **Documents** | 文档管理 | 上传/下载附件、文档预览 |

---

## 三、架构设计

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              前端层                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Flowable Work 官方 UI                             │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │   │
│  │  │   New    │ │   Work   │ │  Tasks   │ │Contacts │ │Documents │  │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      │                                      │
│                              HTTP / REST API                                │
│                                      ▼                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                              后端层                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Spring Boot 4 + JDK 25                            │   │
│  │  ┌─────────────────────────────────────────────────────────────┐    │   │
│  │  │                    Flowable 流程引擎                          │    │   │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │    │   │
│  │  │  │Repository│ │ Runtime  │ │  Task    │ │ History  │        │    │   │
│  │  │  │  Service │ │ Service  │ │ Service  │ │ Service  │        │    │   │
│  │  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │    │   │
│  │  └─────────────────────────────────────────────────────────────┘    │   │
│  │                                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────────┐    │   │
│  │  │              流程定义与扩展（业务集成层）                      │    │   │
│  │  │  - 委托表达式（调用 Spring Bean）                              │    │   │
│  │  │  - 事件监听器（流程/任务事件）                                 │    │   │
│  │  │  - 流程变量与业务数据映射                                      │    │   │
│  │  └─────────────────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      │                                      │
│                              JDBC / ORM                                     │
│                                      ▼                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                              数据层                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         MySQL 数据库                                 │   │
│  │  Flowable 表（约 50+ 张）：ACT_RE_*, ACT_RU_*, ACT_HI_*, ACT_ID_*    │   │
│  │  业务表（自定义）：customer, order, approval_record 等               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 Flowable Work UI 页面结构

Flowable Work UI 的页面布局主要包含以下元素：

| 区域 | 说明 | 可定制性 |
|------|------|---------|
| **侧边栏** | 应用导航（New/Work/Tasks/Contacts/Documents） | 可配置展开/最小化 |
| **顶部导航栏** | 用户菜单、通知、设置 | 默认隐藏，可启用 |
| **收件箱面板** | 任务列表、流程实例列表 | 可自定义列 |
| **详情面板** | 任务详情、表单、历史、附件、人员 | 高度可定制 |
| **弹窗** | 表单弹窗、确认对话框 | 可自定义宽度和样式 |

### 3.3 数据流向

```
[用户请求] → [Flowable Work UI] → [REST API] → [Flowable 引擎] → [数据库]
                                                      ↓
                                            [业务逻辑执行]
                                                      ↓
                                            [任务自动流转]
                                                      ↓
                                            [待办任务通知]
```

---

## 四、Flowable Work UI 集成方案

### 4.1 嵌入式集成（推荐）

Flowable 提供 `flowable-work-frontend` jar 包，可嵌入 Spring Boot 应用运行。

**Maven 依赖**：
```xml
<dependency>
    <groupId>org.flowable</groupId>
    <artifactId>flowable-spring-boot-starter</artifactId>
    <version>8.x</version>
</dependency>
<dependency>
    <groupId>org.flowable</groupId>
    <artifactId>flowable-work-frontend</artifactId>
    <version>8.x</version>
</dependency>
```

**配置**：
```yaml
flowable:
  work:
    frontend:
      enabled: true
      path: /work  # UI 访问路径
```

### 4.2 前端定制扩展

Flowable Work UI 支持通过 `custom.js` 和 `custom.css` 进行扩展。

**项目结构**：
```
src/main/resources/
├── static/
│   └── ext/
│       ├── custom.css      # 自定义样式
│       └── custom.js       # 自定义JavaScript
```

**使用官方扩展工具链**：
```bash
# 初始化扩展项目
mkdir customize-work-fe && cd customize-work-fe
yarn init --yes
yarn add @flowable/work-scripts typescript --dev

# 构建扩展
yarn build
# 生成文件：dist/custom.js, dist/custom.css
```

**扩展能力**：
- 添加自定义应用（FlowApps）
- 添加多语言翻译
- 自定义表单组件
- 自定义头部导航栏
- 添加流程/案例页面组件

### 4.3 独立部署

Flowable Work UI 也可作为独立 Web 应用部署：
- 部署到任意 HTML 服务器（Nginx/Tomcat）
- 后端 Flowable 引擎独立部署
- 前后端通过 REST API 通信

---

## 五、核心业务流程示例

### 5.1 请假审批流程

这是一个最经典的 BPMN 流程示例，帮助你快速理解 Flowable 的工作方式。

**流程设计者** 可以完全通过 Flowable Work UI 的 **New** → **Process** 拖拽设计该流程，无需编写代码。

**流程步骤**：

| 步骤 | 节点类型 | 处理人 | 说明 |
|------|---------|--------|------|
| 1 | 开始事件 | - | 员工发起申请 |
| 2 | 用户任务 | 员工 | 填写请假表单（天数、类型、理由） |
| 3 | 排他网关 | 系统 | 判断：天数 ≤ 3 天走直线主管；> 3 天走部门经理 |
| 4 | 用户任务 | 直线主管 | 主管审批（天数 ≤ 3 天） |
| 5 | 用户任务 | 部门经理 | 经理审批（天数 > 3 天） |
| 6 | 服务任务 | 系统 | 更新请假记录到业务表 |
| 7 | 结束事件 | - | 流程结束 |

**参与者使用 Flowable Work UI 的操作流程**：

1. **员工**在 Work 应用中发起流程 → 填写表单 → 提交
2. **主管/经理**登录后在 Tasks 应用中看到待办任务 → 点击审批 → 填写意见 → 完成
3. **员工**可在 Work 中查看流程进度（高亮当前节点）
4. 流程结束后，**HR** 可在历史记录中查看完整审计日志

### 5.2 与现有客户系统集成

当需要在自定义页面（如公司官网）嵌入流程表单时，可以使用 **Flowable Forms** 组件：

```typescript
// Angular/React/Vue 项目中使用
import { render } from '@flowable/forms';

const formWrapper = render(element, {
  config: formConfig,      // 从 Flowable 获取的表单配置
  payload: initialData,
  onChange: (payload) => {
    // 表单数据变化回调
  }
});
```

**典型集成场景**：
- 公司官网客户服务申请表单 → 自动发起工单流程
- 企业内部 CRM 系统 → 嵌入客户审批流程
- 移动端 App → 使用官方 iOS/Android SDK

---

## 六、部署架构

### 6.1 单机部署（开发/测试）

```yaml
# docker-compose.yml
version: "3.8"
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: flowable
    ports:
      - "3306:3306"

  flowable-server:
    image: flowable/flowable-work:latest
    depends_on:
      - mysql
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/flowable
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root123
    ports:
      - "8081:8080"
```

启动访问：`http://localhost:8081/work`

### 6.2 生产部署（Kubernetes）

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flowable-work
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: flowable-work
        image: flowable/flowable-work:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: url
```

### 6.3 数据库表说明

Flowable 引擎默认使用以下表结构：

| 表前缀 | 说明 | 示例表 |
|--------|------|--------|
| ACT_RE_* | 仓库定义表（流程定义、模型） | ACT_RE_PROCDEF, ACT_RE_MODEL |
| ACT_RU_* | 运行时表（流程实例、任务、变量） | ACT_RU_TASK, ACT_RU_VARIABLE |
| ACT_HI_* | 历史数据表 | ACT_HI_PROCINST, ACT_HI_TASKINST |
| ACT_ID_* | 身份信息表（用户、组） | ACT_ID_USER, ACT_ID_GROUP |
| ACT_DE_* | 决策引擎表（DMN） | ACT_DE_DECISION |

---

## 七、二次开发指南

### 7.1 Spring Bean 调用

在 BPMN 流程中调用 Spring Bean：

```java
@Component("approvalService")
public class ApprovalService {
    
    // 可作为服务任务调用
    public void updateApprovalStatus(String processInstanceId, boolean approved) {
        // 更新业务表
    }
    
    // 可设置流程变量
    public void setApprover(Map<String, Object> variables) {
        variables.put("approver", getCurrentUser());
    }
}
```

BPMN 中配置：
```xml
<serviceTask id="updateStatus" 
             flowable:delegateExpression="${approvalService.updateApprovalStatus(processInstanceId, approved)}" />
```

### 7.2 事件监听器

```java
@Component
public class ProcessEventListener implements FlowableEventListener {
    
    @Override
    public void onEvent(FlowableEvent event) {
        switch (event.getType()) {
            case TASK_COMPLETED:
                // 任务完成后处理
                break;
            case PROCESS_COMPLETED:
                // 流程完成后通知
                break;
        }
    }
}
```

### 7.3 前端扩展

通过 `custom.js` 添加自定义功能：

```javascript
// 注册自定义应用
const applications = [{
    id: 'custom-app',
    title: '业务报表',
    route: '/custom-reports',
    icon: 'report',
    component: () => import('./CustomReports')
}];

export default { applications };
```

---

## 八、版本状态说明

| 配置项 | 版本/状态 | 说明 |
|--------|----------|------|
| Flowable 8 | ✅ 稳定 | 官方支持 Spring Boot 4 + JDK 25 |
| Flowable Work UI | ✅ 官方提供 | 嵌入式或独立部署均可 |
| 流程模型器 | ⚠️ 注意 | 开源版无自托管，需用 BPMN-JS 或企业版 |

> ⚠️ **重要说明**：Flowable 7+ 已将传统 `flowable-modeler` 替换为云端模型器。开源版如需流程建模能力，可使用 `bpmn-js` 开源库，生成的 BPMN XML 可被 Flowable 引擎识别。

### 流程建模方案对比

| 方案 | 适用场景 | 说明 |
|------|---------|------|
| **Flowable Design（企业版）** | 付费商业场景 | 需联系官方获取许可 |
| **bpmn-js（开源）** | 开源版最佳方案 | 生成的 XML 可被 Flowable 使用 |
| **Flowable Work UI** | 运行时查看 | 可查看流程图高亮，不支持编辑 |
| **VS Code插件** | 开发者使用 | 本地编辑 BPMN 文件 |

---

## 九、环境要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | **25** | Flowable 8 需 Java 17+，25 已验证 |
| Spring Boot | **4.0.6** | Flowable 8 完全适配 |
| MySQL | 8.0+ | 支持 JSON 类型的流程变量 |
| 内存 | 最低 2GB，推荐 4GB+ | - |
| 浏览器 | Chrome/Edge/Firefox 最新版 | Flowable Work UI 基于现代浏览器 |

---

## 十、交付物清单

| 交付物 | 说明 |
|--------|------|
| Spring Boot 4 + Flowable 8 基础项目 | Maven/Gradle 项目脚手架 |
| 数据库初始化脚本 | Flowable 8 表结构 DDL |
| Docker Compose 配置 | 一键启动环境 |
| 示例流程 | 请假审批 BPMN 文件 |
| 前端扩展模板 | custom.js/css 模板（可选） |
| 部署文档 | 生产环境部署指南 |

---

## 十一、后续规划

| 阶段 | 周期 | 内容 |
|------|------|------|
| Phase 1 | 第1周 | 环境搭建 + Flowable 基础学习 + 运行官方 Demo |
| Phase 2 | 第2-3周 | 设计典型流程（请假/报销）+ 与业务系统集成 |
| Phase 3 | 第4周 | 前端定制（Logo/主题/自定义应用） |
| Phase 4 | 第5-6周 | 性能测试 + 生产部署准备 |
| Phase 5 | 长期 | 扩展更多流程场景 + 流程监控报表 |

---

## 附录：参考资料

- [Flowable 官方文档](https://documentation.flowable.com/)
- [Flowable Work UI 定制指南](https://documentation.flowable.com/latest/develop/fe/customize)
- [bpmn-js GitHub](https://github.com/bpmn-io/bpmn-js)

---

**文档编制**：许飞杨  
**最后更新**：2026-04-30